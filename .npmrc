# Several deps (swr, react-multi-carousel, ...) work with React 19 but haven't
# widened their peerDependencies ranges yet, so npm's strict peer resolution
# fails. Accept legacy peer resolution so installs succeed consistently across
# local, CI, and Vercel.
legacy-peer-deps=true
